/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ['localhost', 'thuthuatphanmem.vn', 'noitoiseden.com','thuelens.com','khoinguonsangtao.vn'],
  },
}

module.exports = nextConfig
